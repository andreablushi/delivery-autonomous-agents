\section{PDDL-Based Crate Navigation}
\label{sec:pddl}
Some maps in the Deliveroo.js environment contain movable crates that can obstruct navigable paths. 
An agent may push a crate one tile in any cardinal direction, subject to the constraints outlined in Section~\ref{sec:planning}. 
We model this crate manipulation problem using PDDL (Planning Domain Definition Language), invoking it as a fallback mechanism when the standard A* planner fails to find an unobstructed route.

\subsection{Domain Overview}
The \texttt{deliveroo-crates} domain is specified in STRIPS, utilizing two core object types: \texttt{tile} and \texttt{crate}. The predicate set captures the complete state required for crate-aware navigation:
\begin{itemize}[noitemsep]
  \item \texttt{(at ?t)}: the agent occupies tile $t$;
  \item \texttt{(adj-up/down/left/right ?from ?to)}: defines the directed spatial relationship between adjacent tiles, specifying valid movement or pushing directions;
  \item \texttt{(crate-at ?c ?t)}: crate $c$ is positioned on tile $t$;
  \item \texttt{(crate-free ?t)}: tile $t$ currently contains no crate, meaning it is available for agent entry or as a push destination;
  \item \texttt{(crate-space ?t)}: tile $t$ is structurally capable of supporting a crate (e.g., it is not a wall or a conveyor belt).
\end{itemize}
The domain defines eight parameterized actions: four directional \emph{move} actions and four directional \emph{push} actions. 
A move action allows the agent to step into an adjacent \texttt{crate-free} tile. 
A push action allows the agent to displace a crate one tile in the specified direction, requiring two preconditions: 
(i) the agent is positioned adjacent to the crate on the opposite side of the push direction, and
(ii) the target destination is a valid \texttt{crate-space} and is currently \texttt{crate-free}. 
The execution of a push action simultaneously relocates the crate to the target tile and moves the agent onto the crate's former tile, updating the \texttt{crate-free} state of the involved tiles accordingly.

\subsection{Planning With PDDL}
\begin{comment}
#TODO: maybe add more about how we know when to invoke the planner
\end{comment}
PDDL planning is triggered exclusively when A* fails and the belief system confirms that crates obstruct the route (Section~\ref{sec:planning}).

The problem instance is built from the agent's current beliefs: adjacency facts are built for every non-wall tile pair, 
\texttt{crate-at} and \texttt{crate-free} facts for each believed crate position, 
and \texttt{crate-space} facts for all structurally valid positions. 
The goal is a single ground atom \texttt{(at target)}.

The original approach used PDDL to compute a complete path from the agent's current position to the final goal, which was then directly translated into a sequence of \texttt{move} and \texttt{push} steps for execution.

\subsection{A Smart Solution --- Problem Restatement \& Caching}
Since the PDDL planner introduced overhead that was not acceptable for the reactive nature of the environment, we implemented a problem restatement to enable plan caching.

Instead of invoking the planner for a global solution from the agent's current position to the final goal, the plan is decomposed into three steps:
\begin{itemize}[noitemsep]
    \item Move from the current position to the cluster entry tile (A*);
    \item Navigate through the crate cluster (PDDL);
    \item Move from the cluster exit tile to the final goal (A*).
\end{itemize}
The PDDL planner is therefore restricted to the second step alone. Computed maneuvers are stored in a FIFO cache of size 32, keyed by the tuple $(\text{entry},\, \text{exit},\, \{\text{crate positions}\})$. The cache key is independent of the agent's absolute position: it depends only on the relative crate arrangement and the cluster's entry and exit tiles. Consequently, the exact same maneuver plan is reused whenever the agent encounters a structurally identical sub-problem --- including cases where the agent approaches the same cluster from a different direction --- drastically reducing planner invocations and keeping the agent reactive.
